# Proyecto de productos

Maqueta Flutter de categorías y listado de productos. Incluye modelos, servicios HTTP para DummyJSON y proveedores Riverpod; la interfaz actual muestra elementos de ejemplo.

Ejecutar desde esta carpeta: `flutter pub get`, `flutter run`.
Validar: `flutter analyze` y `flutter test`.
